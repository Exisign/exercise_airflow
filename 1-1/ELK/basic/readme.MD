# Opensearch
## basic
- 환경변수 설정
    - OPENSEARCH_HOST 값으로 Opensearch 대시보드에 도메인 엔드포인트(IPv4)의 내용 중 https:// 만 제외한 나머지 값 세팅
        ```
            search-de-ai- ... com
        ```
    - AUTH_NAME, AUTH_PW는 서비스 생성중에 적용한 마스터 계정 정보 적용